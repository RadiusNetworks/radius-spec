inherit_from: .rubocop.yml

inherit_mode:
  merge:
    - Exclude

AllCops:
  Exclude:
    - db/schema.rb
    - db/migrate/*

# Rails project's are not concerned with API docs normally
Documentation:
  Enabled: false
