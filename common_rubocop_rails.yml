inherit_mode:
  merge:
    - Exclude

inherit_from: common_rubocop.yml

AllCops:
  Exclude:
    - 'db/schema.rb'
    - 'db/migrate/**/*'

# Rails project's are not concerned with API docs normally
Documentation:
  Enabled: false
